import React from 'react';

const NotFound = props => {

    return (
        <div className="not-found">
            <h3>Sorry, that page page doesn't seem to exist.</h3>
        </div>
    )};
export default NotFound;